# Model families

- `tabular/`: Ridge, HistGradientBoosting, XGBoost, LightGBM, and similar structured-data models
- `sequence/`: LSTM and other sequential architectures
- `circular/`: wind-direction-specific handling
- `rain/`: rain occurrence / amount specialized logic
- `ensembles/`: stacking or blend layer if justified
